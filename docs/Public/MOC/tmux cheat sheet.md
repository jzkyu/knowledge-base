https://tmuxcheatsheet.com/

`ctrl` + `w`: "tmux mode"

### In "tmux mode":

`w`: Window previewer

`c`: Create window

`p / n`: Prev / next window

`&`: Close window

### Sessions:

`tmux kill-session -a`: Kill all sessions but the current

